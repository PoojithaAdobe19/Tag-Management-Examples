// Adobe Launch sample implementation
document.getElementById('purchaseButton').addEventListener('click', function() {
    // Track the purchase button click event
    _satellite.track('purchaseButtonClick');
});
